CREATE TABLE NAGT_LOG_ENVIO_ACO_EMAIL (
    ID_LOG        NUMBER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    NRO_ACORDO    NUMBER(30),
    COD_COMPRADOR VARCHAR2(50),
    EMAIL_DESTINO VARCHAR2(1000),
    QTDE_ACORDOS  NUMBER,
    HTML_EMAIL    CLOB,
    DATA_ENVIO    DATE DEFAULT SYSDATE
);
